// Pure business logic — no side effects, no DB, no network.
//
// Suggested exports:
//   createEntry(person, beverage, loggedBy)  — build a drink log object
//   getDailySummary(entries)                 — aggregate entries by person
//   getPersonSummary(entries, personId)      — totals for one person

module.exports = {};
